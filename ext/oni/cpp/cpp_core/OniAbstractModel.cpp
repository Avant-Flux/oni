#include "OniAbstractModel.h"

namespace Oni
{
	AbstractModel::AbstractModel()
		// : AbstractAbstractModel()
	{
	}

	AbstractModel::~AbstractModel()
	{
		// mNode->detachObject(mEntity);
		// mSceneMgr->destroyEntity(mEntity);
		// mSceneMgr->destroySceneNode(mNode);
	}
}